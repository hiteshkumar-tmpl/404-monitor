{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspaces --if-present",
    "dev": "concurrently -n api,ui -c blue,cyan \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "cd artifacts/api-server && npm run dev",
    "dev:ui": "cd artifacts/monitor-app && npm run dev"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "concurrently": "^9.1.2"
  },
  "packageManager": "npm@10",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ]
}
